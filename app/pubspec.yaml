name: app
description: "student_performance_tracker"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  get_it: ^7.7.0
  provider: ^6.1.2
  syncfusion_flutter_charts: ^26.2.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/grafik.png
    - assets/sample2.json
